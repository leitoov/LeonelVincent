import presentation from '../constants/presentation.json'
import type { PresentationContent } from '../types/content'

export function getPresentation(): PresentationContent {
  return presentation
}
